# These requirements are for running tests and the scripts in extras, the 
# library itself only needs a subset, see setup.py for that list
coverage==7.0.3
pudb==2022.1.3
pyflakes==3.0.1
Sphinx==6.1.1
sphinx-rtd-theme==0.5.1
tox==4.2.5
twine==4.0.2
